<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.sharefilebc">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShareFileBC"
        tools:targetApi="31">

        <activity
            android:name=".HomeActivity"
            android:exported="true"
            android:launchMode="singleTask">

            <!-- Launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- App Links (https) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="https" android:host="sharefilebcapp.web.app" android:pathPrefix="/download" />
                <data android:scheme="https" android:host="sharefilebcapp.web.app" android:pathPrefix="/folder" />
                <data android:scheme="https" android:host="sharefilebcapp.web.app" android:pathPrefix="/share" />
                <data android:scheme="https" android:host="sharefilebcapp.web.app" android:pathPrefix="/file" />
                <data android:scheme="https" android:host="sharefilebcapp.web.app" android:pathPrefix="/pubkey" />
            </intent-filter>

            <!-- Fallback (http) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="http" android:host="sharefilebcapp.web.app" android:pathPrefix="/download" />
                <data android:scheme="http" android:host="sharefilebcapp.web.app" android:pathPrefix="/folder" />
                <data android:scheme="http" android:host="sharefilebcapp.web.app" android:pathPrefix="/share" />
                <data android:scheme="http" android:host="sharefilebcapp.web.app" android:pathPrefix="/file" />
                <data android:scheme="http" android:host="sharefilebcapp.web.app" android:pathPrefix="/pubkey" />
            </intent-filter>

            <!-- Custom scheme compatibility -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="sharefilebc" android:host="download" />
            </intent-filter>

            <!-- Custom scheme (hosting 404 fallback etc.) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="myapp" android:host="open" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:exported="true" />

        <activity
            android:name=".MainActivity"
            android:exported="false" />

    </application>
</manifest>
